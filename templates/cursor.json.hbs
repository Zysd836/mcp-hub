{
  "mcpServers": {
    "{{serverName}}": {
      "command": "node",
      "args": ["index.js"],
      "env": {
        {{#if hasAuth}}
        "{{authEnvVar}}": "${env:{{authEnvVar}}}"
        {{/if}}
      }
    }
  }
}
