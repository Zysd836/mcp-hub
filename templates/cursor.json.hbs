{
  "mcpServers": {
    "{{serverName}}": {
      "command": "npx",
      "args": ["tsx", "index.ts"],
      "env": {
        {{#if hasAuth}}
        "{{authEnvVar}}": "{{cursorEnvRef}}"
        {{/if}}
      }
    }
  }
}
